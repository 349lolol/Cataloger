[project]
name = "catalogai-mcp"
version = "0.1.0"
description = "MCP Server for CatalogAI - Agentic Procurement Interface"
dependencies = [
    "mcp>=0.1.0",
    "httpx>=0.25.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

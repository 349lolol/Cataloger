[project]
name = "catalogai-mcp"
version = "0.1.0"
description = "MCP Server for CatalogAI with code execution"
dependencies = [
    "mcp>=0.1.0",
    "docker>=7.0.0",
    "httpx>=0.25.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
